// Value unchanged from makeTheme; it only moves here so vars.css and the MUI
// theme read the same number. Reconciling it with the radius scale is a
// separate piece of work.
export const shape = { borderRadius: 6 }
