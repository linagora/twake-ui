{
  "name": "twake-ui",
  "private": true,
  "description": "Twake UI monorepo - Shared UI components and theme system for Twake applications",
  "author": "Twake",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/linagora/twake-ui.git"
  },
  "engines": {
    "node": "24.x",
    "npm": ">=9.0.0"
  },
  "keywords": [
    "twake",
    "ui",
    "monorepo",
    "mui",
    "theme"
  ],
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "lint": "npm run lint --workspaces",
    "lint:fix": "npm run lint --workspaces --if-present -- --fix",
    "clean": "npm run clean --workspaces && rm -rf node_modules",
    "release": "release-it --ci",
    "release:dry": "release-it --ci --dry-run"
  },
  "devDependencies": {
    "@commitlint/cli": "21.0.2",
    "@commitlint/config-conventional": "21.0.2",
    "@release-it-plugins/workspaces": "5.0.3",
    "@release-it/conventional-changelog": "11.0.1",
    "release-it": "19.2.4",
    "typescript": "5.5.2"
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ],
    "rules": {
      "subject-case": [
        2,
        "always",
        "sentence-case"
      ]
    }
  }
}
